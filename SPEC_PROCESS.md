# SPEC_PROCESS.md —— 与 Superpowers 协作生成 spec 与 plan 的过程记录

> 作者：个人项目 · Rampart（AI4SE 期末项目 A · Coding Agent Harness）
> 作用：记录 brainstorming → writing-plans → 冷启动验证的过程证据（通用要求 §4.3 / §4.5、A 项目 §A.7）。

---

## 1. 引言

本项目通过 OpenCode 内置的 Superpowers 技能框架完成。本文件回答两个问题：**brainstorming 如何产出一份足够清晰的 spec**，以及**冷启动验证（陌生 agent 仅凭 SPEC+PLAN）暴露了哪些 spec 缺陷、我如何据此修订**。后者是单人项目里最接近"同侪评审"的客观证据。

---

## 2. brainstorming 关键节点

### 2.1 流程概述

1. 阅读两个要求文件（通用要求 + A 项目说明）。
2. 检查开发环境（node 24 / 无 Docker / 无 Go-Rust / macOS Keychain / git 配置）。
3. 逐题确认 5 个关键决策（每题单选，附理由）。
4. 提出 3 个候选架构，确认 1 个。
5. 分 4 节呈现设计，逐节签字。
6. 落笔 SPEC.md，自审后提交。

### 2.2 智能体追问 / 促使我明确的问题

Superpowers 的 brainstorming 强制"一个问题只问一次、先定范围再问细节"。对我最具价值的几个追问：

1. **"你要交付的是 harness 内核，还是在一个现成 agent 框架上做配置？"** 一开始我倾向于"复用现有循环框架 + 加护栏配置"，追问迫我明确了 §A.4 的核心纪律：主循环、工具分发、护栏、反馈都必须是自己写的代码。这决定了整个项目形态。
2. **"重点维度选择：治理 / 反馈 / 扩展？"** 我最初想平均用力，六维都做。要求文件（§A.4-D）明确"一个维度做深"。选择治理，因为它最符合"移除 LLM 仍可确定性测试"的硬判据。
3. **"key 怎么存？"** 促使我提前设计 Keychain 录入/更新/清除/状态四个命令与威胁模型，而不是最后一刻补。
4. **"HITL 怎么让测试可测？"** 这条促使了 Approver 接口的抽象（CLI 实现 + 内存实现共享同一接口），是冷启动能跑起来的关键。

### 2.3 分块设计签字

采用"写一段、确认一段"的方式：

| 节 | 内容 | 处理决策 |
|----|------|---------|
| 1/4 领域与机制 | 四类机制映射、治理为主要贡献 | 通过 |
| 2/4 架构 | 模块划分、数据流、机制=代码的体现 | 通过 |
| 3/4 治理深度 | 路径围栏+命令规则+HITL+沙箱 | 通过 |
| 4/4 凭据/分发/配置 | Keychain、npm、declarative config | 通过 |

---

## 3. 关键迭代对话节选与处理决策

### 迭代 1 —— 范围收窄：从"六维都做"到"治理做深"

- **我的初想**：做成一个"什么都有"的 harness：记忆用向量库、反馈做多轮修正、编排多 agent。计划横跨四五个工作周。
- **AI 质询**：问"如果记忆/上下文工程为重点，其存储与检索必须自己实现，不得复用框架自带 memory——你能接受这个工程量吗；对你而言哪个维度最能体现工程深度"。
- **我的决策**：接受质询，将**治理**定为重点，其余五维只做可运行的最小实现。理由：① 治理天然由代码构成（纯函数拦截，不含 prompt 依赖）；② 三层的深度（规则/Sandbox/HITL）在两周内可控。

### 迭代 2 —— 架构取舍：函数式 vs 类框架 vs 事件总线

- **候选**：
  - A 纯类型核心（函数+判别联合）——推荐
  - B 框架式 OOP（抽象基类 + 插件）
  - C 事件发布订阅
- **AI 意见**：推荐 A，"每个机制是纯函数时，测试不需要 mock LLM；判别联合让 parse 无法悄悄吞掉 kind"。
- **我的决策**：采纳 A。这是 AI 提出而我直接采纳的建议。理由后来被冷启动验证印证：陌生 agent 直接照 PLAN 就能写出可运行代码。

### 迭代 3 —— HITL 可测试性

- **AI 质疑**："如果审批是终端交互，单元测试怎么跑？"
- **我的决策**：将审批抽象为 `Approver` 接口，CLI/内存双实现。这个决定在冷启动的第 3 节被独立验证（陌生 agent 无任何解释即正确使用 `InMemoryApprover`）。

---

## 4. 冷启动验证（客观证据，通用要求 §4.5）

### 4.1 实验设计

- **主导 agent**：主会话（携带全部 brainstorm 历史与隐性上下文）。
- **陌生 agent**：一个 `general` 类型的新 subagent，全新 session、无任何先前对话与 memory，只读 `SPEC.md` + `PLAN.md` 两文件。
- **任务**：只实现 PLAN Task 2（动作模型与解析）。
- **纪律**：明确要求"遇到不确定立即暂停提问，不得猜测"；如需脚手架允许跳过并注释。

### 4.2 结果：陌生 agent 在哪里暂停、产出了什么

- **暂停与提问：0 处。** 它全程没有停下来请求解释。
- **产出**：实现 `src/actions/types.ts`、`src/actions/parse.ts`、`src/actions/parse.test.ts`，8 个测试用例全部通过（`8 pass / 0 fail`），提交 `429fc86`。
- **解读**：这说明 PLAN 对 Task 2 的颗粒度与完整性足够（代码、路径、验证步骤都是逐字给全的）。但"没提问"并不等于"spec 完美"——它在下节列出了 5 个它猜了/补了的点。

### 4.3 暴露的 5 处 SPEC/PLAN 缺陷与修订

| # | 缺陷 | 性质 | 修订 |
|---|------|------|------|
| 1 | SPEC §3.4/§6.1 中 `note` 无 `tags` 字段，但 PLAN 引入了 `tags?: string[]` 且测试断言它 | SPEC 写错（落后于 PLAN） | **SPEC**：`note` 补 `tags?`；§6.1 数据模型同步注明 |
| 2 | 文本协议 `write_file(...)` 无法携带多行 content，但与 SPEC "两种协议都产出正确 Action" 冲突 | PLAN 缺口 | **SPEC**：明确"文本协议 write_file 的 content 为空、写文件必须用 JSON 形式"；PLAN 测试不做文本写文件断言 |
| 3 | `textAction` 用 `{ ... } as Action` 强转，strict tsc 可能报错（冷启动没跑 typecheck） | 潜在质量 | **PLAN+src**：改为类型安全的 `make(kind, args)` 逐 kind 构造，去掉 `as Action` |
| 4 | PLAN 声明 Task 2 依赖 Task 1（脚手架），但仓库尚无脚手架 | 依赖表述不实 | 记录在案；实现顺序上先建脚手架再全量 typecheck |
| 5 | `{"tool":"stop"}` 无 reason → `reason` 键 present 但值为 undefined | 极小的语义瑕疵 | 接受现状，功能无碍；不改 |

### 4.4 修订前后的关键 diff

**SPEC §3.4（Action 定义）修订前**
```ts
| { id; kind: "note"; text }        // 记忆写入
```
**修订后**
```ts
| { id; kind: "note"; text; tags? }        // 记忆写入；tags 用于记忆检索
```

**SPEC §3.4（解析协议）修订后新增**
> 文本协议局限：`write_file` 的文本形式无法携带多行内容（`content` 为空），写文件内容必须用 JSON 形式。

**src/actions/parse.ts（缺陷 #3）修订前**
```ts
function textAction(kind: Action["kind"], args: Record<string, unknown>): Action {
  return { id: newId(), kind, ...args } as Action;
}
```
**修订后（节选）**
```ts
function make(kind: Action["kind"], args: Record<string, unknown>): Action {
  switch (kind) {
    case "read_file": return { id: newId(), kind, path: String(args.path ?? "") };
    // ... 每个 kind 显式构造
  }
}
```

**结果**：修订后 `node --test src/actions/parse.test.ts` 仍 `8 pass`，且新实现无 `as` 强转，可在严格 tsc 下通过（待 Task 1 脚手架落地后全量验证）。

### 4.5 本次实验的结论

1. **清晰 spec 的价值被证实**：当 task 被拆到"文件路径 + 完整代码 + 验证步骤"这一颗粒度，陌生 agent 可以零提问完成。
2. **"没提问"不等于"没猜测"**：它是靠"从 PLAN 抄代码"闭环的；真正的 spec 缺口（tags、文本协议 content）藏在它与 SPEC 的字面差异里，必须靠 diff 而不是靠对话找出来。这印证了 §4.5 设计"记录差异而非只记录提问"的用意。
3. **我据此对 spec 的投入排序**：类型签名精确性 > 协议描述完整性 > 验收标准。缺陷 1/3 都是类型层面，处理成本也最低收益最高。

---

## 5. 哪些建议是 AI 提出而我采纳的 / 我推翻的

| 建议 | 来源 | 决策 | 理由 |
|------|------|------|------|
| 治理维度做深 | AI 追问+要求文件 | 采纳 | 最符合"机制=代码"判据，工程量可控 |
| 架构 A（纯函数+判别联合） | AI 推荐 | 采纳 | 冷启动证明其可照搬可测 |
| `Approver` 接口抽象 HITL | AI 质疑"终端交互怎么测" | 采纳 | 使 HITL 可单测，陌生 agent 零解释即用 |
| 六个维度都做到"完整"再深入 | 我的初想 | 推翻 | 按 §A.4-D 改为"全维度最小实现 + 单维深入" |
| 用向量库做记忆 | 我的初想 | 推翻 | 超出两周可控范围，且引入外部依赖；改用按需检索 JSON 存储 |

---

## 6. 反思：brainstorming 技能做得好 / 不满意之处

**做得好**
- **强制单问**：避免一次抛 6 个问题让用户疲于应付；每个决策点都有清晰的推荐选项与理由，签字压力小。
- **范围先行**："这是否应拆成子项目"的检查避免了我在大项目上一头扎进细节。
- **硬闸门**：设计未获批前禁止写任何代码——确保"先规约后实现"没有走样。

**不满意**
- **对"无 UI 项目"依然有强烈的视觉化倾向**：技能模板默认假设存在可视化问询环节；对纯 CLI harness 这层过滤是噪音。
- **设计文档默认路径**（`docs/superpowers/specs/YYYY-MM-DD-*.md`）与课程对 SPEC.md/PLAN.md 根目录交付的要求不一致，需要人工改写路径。（按通用要求 §4.2 已覆盖。）
- **没有把"验收标准先行"内建到引导里**：直到 writing-plans 阶段，student 才被迫逐条写验收标准；若 brainstorming 就追问"每个功能的『完成』客观标准是什么"，spec 会更早收敛。

**对整体方法论的批判**
- Superpowers 把 TDD/评审/计划的纪律外置，但"验收标准的质量"仍是人的责任：LLM 可以帮你把标准写具体，却不会替你判断"这一步是否真的算完成"。本次冷启动正是靠人审阅 diff 才抓到 tags / 文本协议这类它"猜对了"、但文档是错的缺口。