# REFLECTION.md —— 反思报告

> ⚠️ **学术规范声明（课程 §6）**：本文正文由本人依据本仓库真实 commit / AGENT_LOG / SPEC_PROCESS 撰写，经 AI 辅助润色并在此标注。提交前请通读复核并删除本声明。字数 1500–2500 字。

---

## 1. 哪些技能发挥最大作用，哪些"形式大于实质"？

- **`brainstorming` 的单问/范围先行/硬闸门**：把模糊的"想做个 agent harness"收敛成可签字的规约。逼我完成了两次关键取舍：从"六维全做"收窄为"治理做深"（§A.4-D），从"复用现成循环框架"改成"主循环/护栏/分发全部自写"。每次只问一个单选、可以对着要求文件逐条给理由，比一次抛六问更能让我想清楚。
- **`writing-plans` 的逐字代码颗粒度**：PLAN Task 2 给出"文件路径＋完整代码＋验证步骤"，陌生 subagent **0 提问**完成 8 个测试（commit `429fc86`），是"规约质量→实现质量"最硬的正面证据。
- **`using-git-worktrees` 的隔离纪律**：主会场留在 `main`，18 个 task 全在独立 worktree，合并冲突面为零。

**形式大于实质**：无 UI 的纯 CLI 项目里，brainstorming 的"视觉化问询"环节是噪音（SPEC_PROCESS §6）；多个 check/评审清单在单人项目里退化成仪式，真正客观的第三方评审只有冷启动那次。

## 2. TDD 在 AI 协作下是阻碍还是放大器？

阶段 2 每个 task 都走"先测试→红→实现→绿"。看起来绕路，但实际**实现期偏差几乎都靠红测试暴露**：最典型是 `NODE_TEST_CONTEXT`——内层 `node --test` 被父运行器当"嵌套"直接 exit 0，真实失败误判为 pass（`7d6b18c`）。这种"测试通过但结果错"正是断言不足的病根，被 TDD 逼了出来。

结论：是**放大器而不阻碍**，但有前置条件——它放大的不是 AI 的聪明，而是"约束与环境的差异"。AI 会顺着 PLAN 抄出"看似正确"的代码，只有红测试能在地层产生真信号；若任务一次性、无约束可验证，TDD 的仪式成本就纯是开销。

## 3. subagent 能自主多久？最优 task 颗粒度？

冷启动中陌生 subagent 在"文件路径＋完整代码＋验证步骤"的 Task 2 上 **0 提问完成**。真正的"偏离"不是跑题而是**约束理解不足**：反馈测试用白名单外的 `node -e`，被默认 deny 挡成 deadlock——它忠实实现了坏 PLAN。

颗粒度结论：最优 task 是"**可独立验证的交付单元**"（一个模块的文件＋测试＋验证命令，约 0.5–2 小时）。再大 subagent 开始猜；8 类工具分发挤进一个 task 就是反例，那次只能由主会话自写。

## 4. SPEC/PLAN 质量如何影响实现质量？

正面：Task 2 零提问。反面两个典型缺口：

1. **`note.tags`**：SPEC 定义 `note` 无 `tags`，PLAN 却引入了且测试断言它。陌生 subagent 抄对了 PLAN、写错了 SPEC——**它靠猜测补上，从未质问不一致**，缺口只能靠人与 PLAN 的 diff 抓出（SPEC_PROCESS §4.3）。印证"不提问 ≠ 没猜测"。
2. **strip-only 约束**：PLAN 多处用 Node 类型剥离不支持的语法（参数属性、`enum`、`#private`），每个对应 task 实现期都要改写（`3a57568`/`07c95bd`/`daf7413`）。

因果链：规约写错→subagent 照抄→错误进入实现→测试红或 diff 暴露→重复迁移成本。**规约层修一行，实现期省一次反复**；优先级：类型签名>协议完整性>验收描述。

## 5. 最有效的 prompt/context 策略？

把"**判定**"从 prompt 里拿出来、写成测试依赖的纯函数：给实现者**逐字代码的 PLAN 步骤**而非接口签名（换来零提问，代价是 subagent 退化为抄写，需事后 diff）；对被测对象，护栏写成**纯函数＋判别联合**、MockLLM 提供确定性响应，使停机/审批/反馈完全不依赖 LLM 输出，89 个测试没有一个"看模型心情"；`Action` 用判别联合编码，parse 无法吞掉某类动作。一句话：prompt 负责让 agent 干活，纯函数负责让测试不看 agent 脸色。

## 6. 凭据与分发迫使我正视了什么？

**Keychain**：未配置 key 时 `run` 的报错路径、`status` 只回显是否配置**绝不回显密钥**、测试注入 exec spy 验证命令交互而**全程不触真实 Keychain**（`6de9a89`）。不分离 exec，我很可能写出测试时真读写钥匙串的版本。

**分发**：零运行时依赖、node≥22 门限、`npm link` 实测 bin 路径、CI 安装冒烟——不要求"必须能分发"，我根本不会想到验证 bin 路径与最低版本。

**通常忽略的**：凭据指令的幂等（重复 setup/clear 后 run 的路径）；运行环境编排本身是工程——`NODE_TEST_CONTEXT` 穿透正是同源问题（沙箱不净化 env 就会污染真实执行）。

## 7. 重做会改什么？对方法论的批判

**重做清单**：把 `erasableSyntaxOnly` 约束提前写进 PLAN 模板（省下 6 处重复语法迁移）；反馈类测试先过护栏语义；把目测 acceptance 提到 spec 层（冷启动 5 缺陷中 3 个是字面差异）。

**方法论批判**：
- **假设 1"评审总有人做"——最不成立**。单人项目里评审退化成 self-review，我不得不用"冷启动陌生 subagent"人工制造第三方视角；方法论未覆盖单人替代机制。
- **假设 2"worktree 必有远端/协作流"**。全程无远端，PR 化流程空转，但隔离价值仍成立。
- **假设 3"单一会话承载全部上下文"**。靠把状态外置到 SPEC/PLAN/AGENT_LOG（文档即上下文）勉强成立，后期上下文仍变钝。
- **假设 4"subagent 与主 agent 能力同构"**。冷启动价值建立在它之上。

**总评**：Superpowers 把 TDD/评审/计划这些在 AI 协作中易松懈的纪律外置成流程，这正是工程师的价值所在；但它只保证"把事做完"，**"验收标准的质量"与"做对了吗"的判断始终是我的责任**。

---
