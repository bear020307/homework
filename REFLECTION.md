# REFLECTION.md —— 反思报告

> ⚠️ **学术规范声明（课程 §6）**：反思报告必须由学生本人撰写，AI 只允许辅助润色并被标注。本文件是**结构模板与提问清单**，以下各处 `<在此填写>` 待学生填写后提交。1500–2500 字。

---

## 0. 撰写说明（交卷前删除本节）

- 下述 7 个小节对应课程建议的全部问题；每节给定了需要回答的具体证据点（都指向本仓库的真实 commit / 文件 / 事件）。
- 请基于真实过程撰写，至少覆盖：技能有效性（形式 vs 实质）、TDD 的放大器/阻碍属性与证据、subagent 自主时长、task 颗粒度、规约质量的因果案例（建议用冷启动 5 缺陷或 `NODE_TEST_CONTEXT`）、prompt 策略、凭据/分发的工程价值、重做清单、方法论批判（假设是否成立）。

---

## 1. 哪些 Superpowers 技能发挥了最大作用，哪些"形式大于实质"？

- 最大作用（建议结合证据）：`brainstorming` 的"单问/范围先行/硬闸门"对规约收敛的作用；`writing-plans` 把 spec 落到逐字代码的颗粒度；`using-git-worktrees` 的隔离纪律（主会场 zero-conflict）。
- 形式大于实质（若有）：<在此填写>。示例论点：无 UI 项目下视觉化问询是噪音（见 SPEC_PROCESS §6）；某些 check 在单人项目里只是仪式。

## 2. TDD 在 AI 协作下是阻碍还是放大器？

- 我的观察：<在此填写>。可用证据：每个 task 严格"先写测试→跑红→实现→跑绿"，而冷启动 subagent 直接照 PLAN 也是一次绿；`NODE_TEST_CONTEXT` 这类"测试通过但结果错"的案例恰是断言不足的表现。
- 结论：<在此填写>。

## 3. subagent-driven 工作流能自主运行多久而不偏离？最优 task 颗粒度？

- 冷启动实验：陌生 subagent 在 Task 2（文件路径+完整代码+验证步骤）上 0 提问完成。
- 偏离案例（若有）：<在此填写>。
- 我的颗粒度结论：<在此填写>。

## 4. SPEC / PLAN 质量如何影响实现质量？给一个"规约不清导致 subagent 偏离"的案例

- 正面证据：<在此填写>。
- 反面/缺口案例（至少举一个）：可选冷启动 5 缺陷中的 `note.tags`（SPEC 落后于 PLAN）或文本协议 write_file 局限；或实现期暴露的 strip-only 不支持 `enum`/参数属性（PLAN 多处代码与 Node 约束冲突）。
- 因果链条：<在此填写>。

## 5. 最有效的 prompt / context 策略是什么、为什么有效？

- <在此填写>。建议对比：逐字代码的 PLAN 步骤 vs 只给接口签名；把"护栏判定"写成纯函数让测试不依赖 LLM。

## 6. 凭据与分发这两条工程要求，迫使你想清楚了哪些原本会忽略的问题？

- Keychain：<在此填写>（未配 key 时 run 的行为、status 不回显、测试注入 exec 不触真实 Keychain 等）。
- 分发：<在此填写>（零运行时依赖、node≥22 门限、npm bin 路径、CI 与冒烟验证）。
- 通常被忽略的：<在此填写>。

## 7. 如果重做会改变什么？对 Superpowers 方法论的批判

### 重做清单
- <在此填写>（如：早期就把 strip-only 约束做进 PLAN 模板；反馈测试先过护栏语义；把目测的 acceptance 提到 spec 层）。

### 方法论批判——它假设了什么？这些假设在本项目成立吗？
- 假设 1：<在此填写>（如"评审有人做"、"worktree 必有远端"）。
- 假设 2：<在此填写>（如"单一 agent 会话可持续承载全部上下文"）。
- 在本项目成立情况：<在此填写>。

---

> 字数统计：完成全部小节后，正文（不含本模板说明）应落在 1500–2500 字。